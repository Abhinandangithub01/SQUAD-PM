version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd client
        - rm -rf amplify/.amplify amplify/node_modules node_modules
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: client/build
    files:
      - '**/*'
