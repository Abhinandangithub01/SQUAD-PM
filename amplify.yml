version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd client
        - rm -rf amplify node_modules
        - npm install --production=false
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: client/build
    files:
      - '**/*'
